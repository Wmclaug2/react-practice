//import React, ReactDOM and App component
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//using the ReactDOM, render the App component
ReactDOM.render(
	//element to render
  <App />,
  //target element to render component in
  document.getElementById('root')
);
